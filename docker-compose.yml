version: "1.0"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: control_Kaiten
    ports:
      - "8000:8000"
    volumes:
      - ./app/configs/config.toml:/app/configs/config.toml
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload